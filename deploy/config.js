window.GHOSTRIDE_BACKEND = {
  // 1. Create a Supabase project.
  // 2. Paste Project URL here, for example: https://your-project.supabase.co
  supabaseUrl: "",

  // 3. Paste the public anon key here. Never paste the service_role key into frontend code.
  supabaseAnonKey: "",

  // When these fields are empty, the site keeps working as a local demo through localStorage.
  demoMode: true
};
